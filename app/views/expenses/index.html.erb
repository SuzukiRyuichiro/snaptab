<div class="flex flex-col w-full gap-4 pb-20">

  <div class="card bg-base-100 w-full shadow-sm">
    <div class="card-body">
      <div class="w-full">
        <%= pie_chart @expenses.group("categories.slug").sum(:amount), prefix: "¥" %>
      </div>
    </div>
  </div>

  <%= render "table", expenses: @expenses || [] %>

</div>
